package com.h2togo.backend.common.enums;

/** Tipo de vehículo (enum nativo {@code tipo_vehiculo} de v6). Etiquetas en minúsculas. */
public enum TipoVehiculo {
    motocicleta,
    automovil,
    camioneta,
    triciclo_carga,
    bicicleta_carga
}
