package com.h2togo.backend.common.enums;

/** Estado de una solicitud de cambio de perfil (enum nativo {@code estado_solicitud} de v6). */
public enum EstadoSolicitud {
    pendiente,
    aprobado,
    rechazado
}
