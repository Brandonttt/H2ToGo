package com.h2togo.backend.common.enums;

/**
 * Tipo de cambio de perfil solicitado (enum nativo {@code codigo_cambio_perfil} de v6).
 * En este enum el esquema define las etiquetas en MAYÚSCULAS, así que las constantes
 * también van en mayúsculas.
 */
public enum CodigoCambioPerfil {
    FOTO_PERFIL,
    DATOS_VEHICULO,
    AGREGAR_VEHICULO,
    ELIMINAR_VEHICULO,
    NOMBRE_NEGOCIO,
    DIRECCION_BASE,
    AGREGAR_PRODUCTO
}
